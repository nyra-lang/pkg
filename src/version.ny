// nyrapkg release version — bump on each tagged release.
const NYRAPKG_VERSION: string = "0.1.0"

const NYRAPKG_REPO: string = "nyra-lang/pkg"
const NYRA_REPO: string = "nyra-lang/nyra"

fn Version_string() -> string {
    return NYRAPKG_VERSION
}
